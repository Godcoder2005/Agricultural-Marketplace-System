<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Details - Farmix</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #2ecc71;
            --secondary-color: #f1c40f;
            --dark-color: #2c3e50;
            --light-color: #ecf0f1;
            --accent-color: #e74c3c;
            --border-radius: 10px;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8f9fa;
            color: #333;
        }

        .navbar {
            padding: 15px 0;
            background: white;
            box-shadow: 0 2px 15px rgba(0,0,0,0.1);
        }

        .navbar-brand {
            font-size: 28px;
            font-weight: 700;
            color: var(--primary-color) !important;
        }

        .nav-link {
            color: var(--dark-color) !important;
            font-weight: 500;
            margin: 0 12px;
            position: relative;
            transition: color 0.3s;
        }

        .nav-link:hover {
            color: var(--primary-color) !important;
        }

        .nav-link.active {
            color: var(--primary-color) !important;
            font-weight: 600;
        }

        .nav-link.active::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 100%;
            height: 3px;
            background-color: var(--primary-color);
            border-radius: 3px;
        }

        /* Order Detail Section */
        .order-detail-container {
            background: white;
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.07);
            margin: 40px auto;
        }

        .order-header {
            background-color: var(--primary-color);
            color: white;
            padding: 25px;
            position: relative;
        }

        .order-header h1 {
            font-size: 28px;
            font-weight: 600;
            margin: 0;
        }

        .order-id {
            font-size: 18px;
            margin-top: 5px;
            opacity: 0.9;
        }

        .order-date {
            position: absolute;
            top: 25px;
            right: 25px;
            font-size: 16px;
            opacity: 0.9;
        }

        .order-status-badge {
            display: inline-block;
            padding: 8px 18px;
            border-radius: 50px;
            font-size: 14px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-top: 10px;
            background-color: white;
        }

        .status-pending {
            color: #d35400;
        }

        .status-processing {
            color: #00cec9;
        }
        
        .status-shipped {
            color: #0984e3;
        }

        .status-delivered {
            color: #00b894;
        }

        .status-completed {
            color: #00b894;
        }

        .status-cancelled {
            color: #d63031;
        }

        .section-title {
            font-size: 20px;
            font-weight: 600;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid var(--light-color);
            color: var(--dark-color);
        }

        .order-section {
            padding: 25px;
        }

        .order-product-card {
            display: flex;
            background: var(--light-color);
            border-radius: var(--border-radius);
            overflow: hidden;
            margin-bottom: 20px;
        }

        .product-image {
            width: 150px;
            height: 150px;
            object-fit: cover;
        }

        .product-details {
            padding: 20px;
            flex: 1;
        }

        .product-name {
            font-size: 20px;
            font-weight: 600;
            margin: 0 0 5px 0;
            color: var(--dark-color);
        }

        .product-category {
            color: #777;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 10px;
        }

        .product-seller {
            margin-bottom: 10px;
        }

        .product-price {
            font-size: 18px;
            font-weight: 600;
            color: var(--primary-color);
        }

        .product-quantity {
            margin-top: 10px;
            font-weight: 500;
        }

        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
        }

        .info-box {
            background: var(--light-color);
            padding: 20px;
            border-radius: var(--border-radius);
        }

        .info-label {
            font-weight: 600;
            color: var(--dark-color);
            margin-bottom: 5px;
        }

        .info-value {
            color: #555;
        }

        .payment-info {
            margin-top: 10px;
            padding-top: 10px;
            border-top: 1px solid #ddd;
        }

        .total-section {
            text-align: right;
            padding: 20px 25px;
            background-color: #f9f9f9;
            border-top: 1px solid #e0e0e0;
        }

        .total-line {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 8px;
        }

        .total-label {
            color: #777;
            margin-right: 20px;
            font-weight: 500;
        }

        .total-value {
            font-weight: 600;
            color: var(--dark-color);
            width: 120px;
            text-align: right;
        }

        .grand-total {
            font-size: 20px;
            font-weight: 700;
            color: var(--primary-color);
        }

        .actions-section {
            padding: 20px 25px;
            border-top: 1px solid #e0e0e0;
            display: flex;
            justify-content: space-between;
        }

        .btn-back {
            background-color: var(--light-color);
            color: var(--dark-color);
            border: none;
            padding: 10px 20px;
            border-radius: 50px;
            font-weight: 500;
            transition: all 0.3s;
            text-decoration: none;
        }

        .btn-back:hover {
            background-color: #dfe6e9;
            color: var(--dark-color);
        }

        .btn-contact-seller {
            background-color: var(--primary-color);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 50px;
            font-weight: 500;
            transition: all 0.3s;
            text-decoration: none;
        }

        .btn-contact-seller:hover {
            background-color: #27ae60;
            color: white;
        }

        /* Responsive adjustments */
        @media (max-width: 767px) {
            .order-date {
                position: static;
                display: block;
                margin-top: 10px;
            }
            
            .info-grid {
                grid-template-columns: 1fr;
                gap: 20px;
            }
            
            .product-image {
                width: 100px;
                height: 100px;
            }
            
            .product-name {
                font-size: 18px;
            }
            
            .total-section {
                padding: 15px;
            }
        }

        .product-info {
            background-color: white;
            border-radius: 15px;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.1);
            padding: 25px;
            margin-bottom: 30px;
        }
        
        .order-item {
            padding: 15px;
            border-bottom: 1px solid #eee;
            margin-bottom: 15px;
        }
        
        .order-item:last-child {
            border-bottom: none;
            margin-bottom: 0;
        }
        
        .item-name {
            font-size: 1.2rem;
            font-weight: 600;
            margin-bottom: 5px;
            color: var(--dark-color);
        }
        
        .item-category {
            font-size: 0.9rem;
            color: #888;
            text-transform: uppercase;
            margin-bottom: 5px;
        }
        
        .item-seller {
            font-size: 0.9rem;
            color: #888;
            margin-bottom: 0;
        }
        
        .item-quantity, .item-price, .item-total {
            font-size: 0.9rem;
            margin-bottom: 5px;
        }
        
        .item-total {
            font-weight: 600;
            color: var(--dark-color);
        }

        .item-description {
            padding: 10px;
            background-color: #f8f9fa;
            border-radius: 5px;
            font-size: 0.9rem;
        }
        
        .item-description h6 {
            font-size: 0.95rem;
            margin-bottom: 5px;
            color: #555;
        }
        
        .order-summary {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin-top: 20px;
        }
        
        .order-summary h4 {
            margin-bottom: 15px;
            color: var(--dark-color);
            border-bottom: 1px solid #dee2e6;
            padding-bottom: 10px;
        }
        
        .order-summary .total {
            font-weight: 700;
            color: var(--dark-color);
            font-size: 1.1rem;
            border-top: 1px solid #dee2e6;
            padding-top: 5px;
            margin-top: 5px;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="/">Farmix</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/shop">Shop</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/products">Products</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/orders">My Orders</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/about">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contact">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link btn btn-login" href="/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Order Detail Content -->
    <div class="container">
        <div class="order-detail-container">
            <div class="order-header">
                <h1>Order Details</h1>
                <div class="order-id">Order #<%= order.order_id || 'Unknown' %></div>
                <div class="order-date">Placed on <%= order.createdAt ? new Date(order.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) : 'Unknown date' %></div>
                <span class="order-status-badge status-<%= order.order_status ? order.order_status.toLowerCase() : 'pending' %>">
                    <%= order.order_status ? (order.order_status.charAt(0).toUpperCase() + order.order_status.slice(1)) : 'Pending' %>
                </span>
            </div>

            <!-- Product Information -->
            <div class="product-info">
                <% if (order.is_multi_item) { %>
                    <h2 class="section-title">Order Items</h2>
                    <% order.items.forEach(item => { %>
                        <div class="order-item">
                            <div class="row">
                                <div class="col-md-8">
                                    <h3 class="item-name"><%= item.Product.name %></h3>
                                    <p class="item-category"><%= item.Product.category %></p>
                                    <p class="item-seller">Sold by <%= item.Seller.name %> (<a href="mailto:<%= item.Seller.email %>"><%= item.Seller.email %></a>)</p>
                                    <% if (item.Product.description) { %>
                                        <div class="item-description mt-2">
                                            <h6>Product Description</h6>
                                            <p><%= item.Product.description %></p>
                                        </div>
                                    <% } %>
                                </div>
                                <div class="col-md-4 text-end">
                                    <div class="item-quantity">Quantity: <%= item.quantity %></div>
                                    <div class="item-price">Price: ₹<%= Number(item.price).toFixed(2) %></div>
                                    <div class="item-total">Subtotal: ₹<%= Number(item.total_price).toFixed(2) %></div>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                    
                    <div class="order-summary">
                        <h4>Order Summary</h4>
                        <div class="row">
                            <div class="col-md-8 text-end">
                                <p>Subtotal:</p>
                                <p>Shipping:</p>
                                <p>Tax (5%):</p>
                                <p class="total">Total:</p>
                            </div>
                            <div class="col-md-4 text-end">
                                <% let subtotal = order.items.reduce((sum, item) => sum + Number(item.total_price), 0); %>
                                <% let shipping = 50; %>
                                <% let tax = subtotal * 0.05; %>
                                <% let total = subtotal + shipping + tax; %>
                                <p>₹<%= subtotal.toFixed(2) %></p>
                                <p>₹<%= shipping.toFixed(2) %></p>
                                <p>₹<%= tax.toFixed(2) %></p>
                                <p class="total">₹<%= total.toFixed(2) %></p>
                            </div>
                        </div>
                    </div>
                <% } else { %>
                    <h2 class="section-title">Product Information</h2>
                    <div class="product-name"><%= order.Product.name %></div>
                    <div class="product-category"><%= order.Product.category %></div>
                    <div class="product-seller">Sold by <%= order.Seller.name %></div>
                    <div class="product-quantity">Quantity: <%= order.quantity %></div>
                    <div class="product-price">Unit Price: ₹<%= Number(order.Product.price).toFixed(2) %></div>
                    
                    <div class="product-description">
                        <h3>Product Description</h3>
                        <p><%= order.Product.description || 'No description available.' %></p>
                    </div>
                <% } %>
            </div>

            <!-- Order Information -->
            <div class="order-section">
                <h2 class="section-title">Order Information</h2>
                <div class="info-grid">
                    <div class="info-box">
                        <h3 class="section-title">Shipping Details</h3>
                        <div>
                            <div class="info-label">Full Name</div>
                            <div class="info-value"><%= order.first_name || '' %> <%= order.last_name || '' %></div>
                        </div>
                        <div class="mt-3">
                            <div class="info-label">Email</div>
                            <div class="info-value"><%= order.email || 'No email provided' %></div>
                        </div>
                        <div class="mt-3">
                            <div class="info-label">Phone</div>
                            <div class="info-value"><%= order.phone || 'No phone provided' %></div>
                        </div>
                        <div class="mt-3">
                            <div class="info-label">Address</div>
                            <div class="info-value">
                                <%= order.address || 'No address provided' %><br>
                                <%= order.city || '' %><%= order.city && order.pincode ? ', ' : '' %><%= order.pincode || '' %>
                            </div>
                        </div>
                    </div>
                    <div class="info-box">
                        <h3 class="section-title">Payment Information</h3>
                        <div>
                            <div class="info-label">Payment Status</div>
                            <div class="info-value">
                                <span class="order-status payment-<%= order.payment_status ? order.payment_status.toLowerCase() : 'pending' %>">
                                    <%= order.payment_status ? (order.payment_status.charAt(0).toUpperCase() + order.payment_status.slice(1)) : 'Pending' %>
                                </span>
                            </div>
                        </div>
                        <div class="mt-3">
                            <div class="info-label">Payment Method</div>
                            <div class="info-value">Credit/Debit Card</div>
                        </div>
                        <div class="payment-info">
                            <div class="info-label">Payment Date</div>
                            <div class="info-value"><%= order.createdAt ? new Date(order.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) : 'Unknown date' %></div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Order Summary -->
            <div class="total-section">
                <div class="total-line">
                    <div class="total-label">Subtotal</div>
                    <div class="total-value">₹<%= order.total_price ? Number(order.total_price).toFixed(2) : '0.00' %></div>
                </div>
                <div class="total-line">
                    <div class="total-label">Shipping</div>
                    <div class="total-value">₹0.00</div>
                </div>
                <div class="total-line">
                    <div class="total-label">Tax</div>
                    <div class="total-value">₹0.00</div>
                </div>
                <div class="total-line">
                    <div class="total-label grand-total">Total</div>
                    <div class="total-value grand-total">₹<%= order.total_price ? Number(order.total_price).toFixed(2) : '0.00' %></div>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="actions-section">
                <a href="/orders" class="btn-back">
                    <i class="fas fa-arrow-left me-2"></i>Back to Orders
                </a>
                <a href="mailto:<%= order.Seller.email %>" class="btn-contact-seller">
                    <i class="fas fa-envelope me-2"></i>Contact Seller
                </a>
            </div>

            <div class="text-center mb-4">
                <a href="/orders" class="btn btn-outline-primary mb-2 ms-2"><i class="fas fa-arrow-left"></i> Back to Orders</a>
                <a href="/order-tracking/<%= order.order_id %>" class="btn btn-outline-success mb-2 ms-2"><i class="fas fa-truck-loading"></i> Track Order</a>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-dark text-light py-5 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h5>About Farmix</h5>
                    <p>Your trusted agricultural marketplace connecting farmers and buyers.</p>
                </div>
                <div class="col-md-4">
                    <h5>Quick Links</h5>
                    <ul class="list-unstyled">
                        <li><a href="/" class="text-light">Home</a></li>
                        <li><a href="/shop" class="text-light">Shop</a></li>
                        <li><a href="/products" class="text-light">Products</a></li>
                        <li><a href="/services" class="text-light">Services</a></li>
                        <li><a href="/about" class="text-light">About</a></li>
                        <li><a href="/contact" class="text-light">Contact</a></li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h5>Contact Us</h5>
                    <p>Email: info@farmix.com<br>
                    Phone: +1 234 567 890</p>
                </div>
            </div>
            <div class="text-center mt-4">
                <p>&copy; 2024 Farmix. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 