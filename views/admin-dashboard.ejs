<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Agricultural Marketplace</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #199733;
            --dark-bg: #170f29;
            --input-bg: rgba(255, 255, 255, 0.1);
            --text-light: #ffffff;
        }

        body {
            min-height: 100vh;
            background: linear-gradient(rgba(23, 15, 41, 0.85), rgba(23, 15, 41, 0.95)),
                        url('https://images.unsplash.com/photo-1625246333195-78d9c38ad449?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1920&q=80');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            font-family: 'Poppins', sans-serif;
            color: var(--text-light);
        }

        .navbar {
            background: rgba(23, 15, 41, 0.9);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .notification-btn {
            position: relative;
            background: none;
            border: none;
            color: var(--text-light);
            font-size: 1.2rem;
            padding: 0.5rem;
            cursor: pointer;
            transition: transform 0.3s;
        }

        .notification-btn:hover {
            transform: scale(1.1);
        }

        .notification-badge {
            position: absolute;
            top: 0;
            right: 0;
            background: #dc3545;
            color: white;
            border-radius: 50%;
            padding: 0.25rem 0.5rem;
            font-size: 0.75rem;
        }

        .messages-container {
            position: fixed;
            right: 20px;
            top: 80px;
            width: 400px;
            max-height: 80vh;
            background: rgba(23, 15, 41, 0.95);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            backdrop-filter: blur(10px);
            display: none;
            z-index: 1000;
            overflow-y: auto;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }

        .messages-container.show {
            display: block;
            animation: slideIn 0.3s ease-out;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateX(20px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        .messages-header {
            padding: 1rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .messages-header h5 {
            margin: 0;
            color: var(--primary-color);
        }

        .close-messages {
            background: none;
            border: none;
            color: var(--text-light);
            font-size: 1.5rem;
            cursor: pointer;
            padding: 0;
            opacity: 0.7;
            transition: opacity 0.3s;
        }

        .close-messages:hover {
            opacity: 1;
        }

        .messages-list {
            padding: 1rem;
        }

        .message-item {
            padding: 1rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            transition: background-color 0.3s;
            cursor: pointer;
        }

        .message-item:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        .message-item:last-child {
            border-bottom: none;
        }

        .message-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.5rem;
        }

        .message-subject {
            font-weight: 600;
            color: var(--primary-color);
        }

        .message-time {
            font-size: 0.8rem;
            opacity: 0.7;
        }

        .message-content {
            margin-bottom: 0.5rem;
        }

        .message-preview {
            font-size: 0.9rem;
            opacity: 0.8;
        }

        .no-messages {
            text-align: center;
            padding: 2rem;
            opacity: 0.7;
        }

        .welcome-card {
            background: rgba(23, 15, 41, 0.7);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 2rem;
            text-align: center;
            margin-top: 2rem;
            backdrop-filter: blur(10px);
        }

        .welcome-title {
            color: var(--primary-color);
            margin-bottom: 1rem;
        }

        .welcome-text {
            opacity: 0.8;
            margin-bottom: 0;
        }

        .message-details-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            z-index: 2000;
            backdrop-filter: blur(5px);
        }

        .message-details-modal.show {
            display: flex;
            animation: fadeIn 0.3s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        .modal-content {
            background: rgba(23, 15, 41, 0.95);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            width: 90%;
            max-width: 800px;
            margin: auto;
            max-height: 90vh;
            overflow-y: auto;
            backdrop-filter: blur(10px);
            box-shadow: 0 5px 25px rgba(0, 0, 0, 0.5);
        }

        .modal-header {
            background: rgba(25, 151, 51, 0.2);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            padding: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .modal-title {
            color: var(--primary-color);
            margin: 0;
        }

        .close-modal {
            background: none;
            border: none;
            color: var(--text-light);
            font-size: 1.5rem;
            cursor: pointer;
            padding: 0;
            opacity: 0.7;
            transition: opacity 0.3s;
        }

        .close-modal:hover {
            opacity: 1;
        }

        .modal-body {
            padding: 1.5rem;
        }

        .message-details-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1rem;
        }

        .message-details-table th,
        .message-details-table td {
            padding: 0.75rem;
            text-align: left;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .message-details-table th {
            background: rgba(25, 151, 51, 0.1);
            color: var(--primary-color);
            font-weight: 600;
        }

        .message-details-table tr:last-child td {
            border-bottom: none;
        }

        .message-details-table tr:hover td {
            background: rgba(255, 255, 255, 0.05);
        }

        /* Admin Dashboard Tabs */
        .admin-tabs {
            display: flex;
            justify-content: center;
            margin-top: 2rem;
            margin-bottom: 2rem;
        }

        .admin-tab {
            background: rgba(23, 15, 41, 0.7);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: var(--text-light);
            padding: 0.75rem 1.5rem;
            margin: 0 0.5rem;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .admin-tab:hover {
            background: rgba(25, 151, 51, 0.3);
        }

        .admin-tab.active {
            background: var(--primary-color);
            border-color: var(--primary-color);
        }

        /* Products Table */
        .products-table-container {
            background: rgba(23, 15, 41, 0.7);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 1.5rem;
            margin-top: 1rem;
            backdrop-filter: blur(10px);
        }

        .products-table {
            width: 100%;
            border-collapse: collapse;
            color: var(--text-light);
        }

        .products-table th,
        .products-table td {
            padding: 0.75rem;
            text-align: left;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .products-table th {
            background: rgba(25, 151, 51, 0.2);
            color: var(--primary-color);
            font-weight: 600;
        }

        .products-table tr:hover td {
            background: rgba(255, 255, 255, 0.05);
        }

        .delete-btn {
            background: #dc3545;
            color: white;
            border: none;
            border-radius: 4px;
            padding: 0.25rem 0.5rem;
            cursor: pointer;
            transition: all 0.3s;
        }

        .delete-btn:hover {
            background: #c82333;
        }

        /* Confirmation Modal */
        .confirmation-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            z-index: 3000;
            backdrop-filter: blur(5px);
        }

        .confirmation-modal.show {
            display: flex;
            animation: fadeIn 0.3s ease-out;
        }

        .confirmation-content {
            background: rgba(23, 15, 41, 0.95);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            width: 90%;
            max-width: 500px;
            margin: auto;
            backdrop-filter: blur(10px);
            box-shadow: 0 5px 25px rgba(0, 0, 0, 0.5);
        }

        .confirmation-header {
            background: rgba(220, 53, 69, 0.2);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            padding: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .confirmation-title {
            color: #dc3545;
            margin: 0;
        }

        .confirmation-body {
            padding: 1.5rem;
            text-align: center;
        }

        .confirmation-actions {
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin-top: 1.5rem;
        }

        .btn-cancel {
            background: rgba(255, 255, 255, 0.1);
            color: var(--text-light);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 4px;
            padding: 0.5rem 1.5rem;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn-cancel:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        .btn-confirm {
            background: #dc3545;
            color: white;
            border: none;
            border-radius: 4px;
            padding: 0.5rem 1.5rem;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn-confirm:hover {
            background: #c82333;
        }
    </style>
</head>
<body>
    <!-- Hidden element to store messages data -->
    <div id="messages-data" data-messages='<%- JSON.stringify(messages || []) %>' style="display: none;"></div>

    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand text-light" href="#">Admin Dashboard</a>
            <div class="ms-auto d-flex align-items-center">
                <button class="notification-btn me-3" id="notificationBtn">
                    <i class="fas fa-bell"></i>
                    <% if (contactMessages && contactMessages.length > 0) { %>
                        <span class="notification-badge"><%= contactMessages.length %></span>
                    <% } %>
                </button>
                <a href="/admin/logout" class="btn btn-outline-light">Logout</a>
            </div>
        </div>
    </nav>

    <!-- Messages Container -->
    <div class="messages-container" id="messagesContainer">
        <div class="messages-header">
            <h5>Contact Messages</h5>
            <button class="close-messages">&times;</button>
        </div>
        <div class="messages-list">
            <% console.log('Template contactMessages:', contactMessages); %>
            <% if (contactMessages && contactMessages.length > 0) { %>
                <% contactMessages.forEach(message => { %>
                    <div class="message-item" data-message-id="<%= message.id %>">
                        <div class="message-header">
                            <span class="message-subject"><%= message.subject %></span>
                            <span class="message-time"><%= new Date(message.created_at).toLocaleString() %></span>
                        </div>
                        <div class="message-content">
                            <strong>From:</strong> <%= message.name %> (<%= message.email %>)
                        </div>
                        <div class="message-preview">
                            <%= message.message.substring(0, 100) %>...
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="no-messages">No messages</div>
            <% } %>
        </div>
    </div>

    <!-- Message Details Modal -->
    <div class="message-details-modal" id="messageDetailsModal">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Message Details</h5>
                <button class="close-modal">&times;</button>
            </div>
            <div class="modal-body">
                <table class="message-details-table">
                    <tr>
                        <th>From:</th>
                        <td id="modal-sender"></td>
                    </tr>
                    <tr>
                        <th>Email:</th>
                        <td id="modal-email"></td>
                    </tr>
                    <tr>
                        <th>Subject:</th>
                        <td id="modal-subject"></td>
                    </tr>
                    <tr>
                        <th>Date:</th>
                        <td id="modal-date"></td>
                    </tr>
                    <tr>
                        <th>Message:</th>
                        <td id="modal-message"></td>
                    </tr>
                </table>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="welcome-card">
            <h1 class="welcome-title">Welcome to Admin Dashboard</h1>
            <p class="welcome-text">Manage your Agricultural Marketplace system from here.</p>
            <% if (messages && messages.length > 0) { %>
                <p class="welcome-text mt-3">
                    You have <%= messages.length %> new message<%= messages.length > 1 ? 's' : '' %>.
                    Click the bell icon to view them.
                </p>
            <% } %>
        </div>

        <!-- Admin Tabs -->
        <div class="admin-tabs">
            <div class="admin-tab active" data-tab="dashboard">Dashboard</div>
            <div class="admin-tab" data-tab="products">Manage Products</div>
            <div class="admin-tab" data-tab="users">Manage Users</div>
        </div>

        <!-- Dashboard Tab Content -->
        <div class="tab-content" id="dashboard-tab">
            <div class="products-table-container">
                <h3 class="mb-4">System Overview</h3>
                <div class="row">
                    <div class="col-md-4 mb-3">
                        <div class="card bg-dark text-light">
                            <div class="card-body text-center">
                                <h5 class="card-title">Total Products</h5>
                                <p class="card-text display-4"><%= productStats.total_products || 0 %></p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4 mb-3">
                        <div class="card bg-dark text-light">
                            <div class="card-body text-center">
                                <h5 class="card-title">Total Orders</h5>
                                <p class="card-text display-4"><%= orderStats.total_orders || 0 %></p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4 mb-3">
                        <div class="card bg-dark text-light">
                            <div class="card-body text-center">
                                <h5 class="card-title">Total Users</h5>
                                <p class="card-text display-4"><%= userStats.total_users || 0 %></p>
                            </div>
                        </div>
                    </div>
                </div>
                
                <h3 class="mb-3 mt-4">Orders Overview</h3>
                <div class="row">
                    <div class="col-md-3 mb-3">
                        <div class="card border-left-info shadow h-100 py-2">
                            <div class="card-body">
                                <div class="row no-gutters align-items-center">
                                    <div class="col mr-2">
                                        <div class="text-xs font-weight-bold text-info text-uppercase mb-1">Revenue</div>
                                        <div class="h5 mb-0 font-weight-bold text-gray-800">₹<%= Number(orderStats.total_revenue || 0).toFixed(2) %></div>
                                    </div>
                                    <div class="col-auto">
                                        <i class="fas fa-dollar-sign fa-2x text-gray-300"></i>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3 mb-3">
                        <div class="card border-left-primary shadow h-100 py-2">
                            <div class="card-body">
                                <div class="row no-gutters align-items-center">
                                    <div class="col mr-2">
                                        <div class="text-xs font-weight-bold text-primary text-uppercase mb-1">Single Items</div>
                                        <div class="h5 mb-0 font-weight-bold text-gray-800"><%= orderStats.single_item_orders || 0 %></div>
                                    </div>
                                    <div class="col-auto">
                                        <i class="fas fa-shopping-bag fa-2x text-gray-300"></i>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3 mb-3">
                        <div class="card border-left-success shadow h-100 py-2">
                            <div class="card-body">
                                <div class="row no-gutters align-items-center">
                                    <div class="col mr-2">
                                        <div class="text-xs font-weight-bold text-success text-uppercase mb-1">Multi Items</div>
                                        <div class="h5 mb-0 font-weight-bold text-gray-800"><%= orderStats.multi_item_orders || 0 %></div>
                                    </div>
                                    <div class="col-auto">
                                        <i class="fas fa-shopping-cart fa-2x text-gray-300"></i>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3 mb-3">
                        <div class="card border-left-warning shadow h-100 py-2">
                            <div class="card-body">
                                <div class="row no-gutters align-items-center">
                                    <div class="col mr-2">
                                        <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">Pending</div>
                                        <div class="h5 mb-0 font-weight-bold text-gray-800"><%= orderStats.pending_orders || 0 %></div>
                                    </div>
                                    <div class="col-auto">
                                        <i class="fas fa-clock fa-2x text-gray-300"></i>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Products Tab Content -->
        <div class="tab-content" id="products-tab" style="display: none;">
            <div class="products-table-container">
                <h3 class="mb-4">Product Management</h3>
                <div class="row mb-4">
                    <div class="col-md-6">
                        <div class="card shadow mb-4">
                            <div class="card-header py-3">
                                <h6 class="m-0 font-weight-bold text-primary">Product Stats</h6>
                            </div>
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <div class="card bg-light">
                                            <div class="card-body text-center">
                                                <h6 class="card-title">Total Products</h6>
                                                <p class="card-text h4"><%= productStats.total_products || 0 %></p>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <div class="card bg-light">
                                            <div class="card-body text-center">
                                                <h6 class="card-title">Total Inventory</h6>
                                                <p class="card-text h4"><%= productStats.total_inventory || 0 %> units</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="card shadow mb-4">
                            <div class="card-header py-3">
                                <h6 class="m-0 font-weight-bold text-primary">Popular Products</h6>
                            </div>
                            <div class="card-body">
                                <div class="table-responsive">
                                    <table class="table table-bordered">
                                        <thead>
                                            <tr>
                                                <th>Name</th>
                                                <th>Category</th>
                                                <th>Price</th>
                                                <th>Qty Sold</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% if (popularProducts && popularProducts.length > 0) { %>
                                                <% popularProducts.forEach(product => { %>
                                                    <tr>
                                                        <td><%= product.name %></td>
                                                        <td><%= product.category %></td>
                                                        <td>₹<%= Number(product.price).toFixed(2) %></td>
                                                        <td><%= product.quantity_sold || 0 %></td>
                                                    </tr>
                                                <% }); %>
                                            <% } else { %>
                                                <tr>
                                                    <td colspan="4" class="text-center">No products found</td>
                                                </tr>
                                            <% } %>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <a href="/admin/products" class="btn btn-primary">Manage All Products</a>
            </div>
        </div>

        <!-- Users Tab Content -->
        <div class="tab-content" id="users-tab" style="display: none;">
            <div class="products-table-container">
                <h3 class="mb-4">User Management</h3>
                <div class="row mb-4">
                    <div class="col-md-4">
                        <div class="card bg-primary text-white mb-4">
                            <div class="card-body text-center">
                                <h6 class="card-title">Total Users</h6>
                                <p class="card-text h2"><%= userStats.total_users || 0 %></p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="card bg-success text-white mb-4">
                            <div class="card-body text-center">
                                <h6 class="card-title">Buyers</h6>
                                <p class="card-text h2"><%= userStats.buyers || 0 %></p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="card bg-info text-white mb-4">
                            <div class="card-body text-center">
                                <h6 class="card-title">Farmers</h6>
                                <p class="card-text h2"><%= userStats.farmers || 0 %></p>
                            </div>
                        </div>
                    </div>
                </div>
                <a href="/admin/users" class="btn btn-primary">Manage All Users</a>
            </div>
        </div>

        <div class="card shadow mb-4">
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                <h6 class="m-0 font-weight-bold text-primary">Recent Orders</h6>
                <a href="/admin/orders" class="btn btn-sm btn-primary">View All</a>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>Order ID</th>
                                <th>Buyer</th>
                                <th>Product</th>
                                <th>Total</th>
                                <th>Status</th>
                                <th>Date</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (recentOrders && recentOrders.length > 0) { %>
                                <% recentOrders.forEach(order => { %>
                                    <tr>
                                        <td>#<%= order.order_id %></td>
                                        <td><%= order.buyer_name %></td>
                                        <td><%= order.product_name %></td>
                                        <td>₹<%= Number(order.total_price).toFixed(2) %></td>
                                        <td>
                                            <span class="badge badge-<%= order.order_status %>">
                                                <%= order.order_status.charAt(0).toUpperCase() + order.order_status.slice(1) %>
                                            </span>
                                        </td>
                                        <td><%= new Date(order.createdAt).toLocaleDateString() %></td>
                                    </tr>
                                <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="6" class="text-center">No recent orders</td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
                <a href="/admin/orders" class="btn btn-info mt-3">Manage Orders</a>
            </div>
        </div>
    </div>

    <!-- Confirmation Modal -->
    <div class="confirmation-modal" id="confirmationModal">
        <div class="confirmation-content">
            <div class="confirmation-header">
                <h5 class="confirmation-title">Confirm Deletion</h5>
                <button class="close-modal" id="closeConfirmationModal">&times;</button>
            </div>
            <div class="confirmation-body">
                <p>Are you sure you want to delete this product?</p>
                <p id="product-to-delete"></p>
                <div class="confirmation-actions">
                    <button class="btn-cancel" id="cancelDelete">Cancel</button>
                    <button class="btn-confirm" id="confirmDelete">Delete</button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const notificationBtn = document.getElementById('notificationBtn');
        const confirmationModal = document.getElementById('confirmationModal');
        const closeConfirmationModal = document.getElementById('closeConfirmationModal');
        const cancelDelete = document.getElementById('cancelDelete');
        const confirmDelete = document.getElementById('confirmDelete');
        const productToDelete = document.getElementById('product-to-delete');
        const messagesContainer = document.getElementById('messagesContainer');
        const messageDetailsModal = document.getElementById('messageDetailsModal');
        const closeMessagesBtn = document.querySelector('.close-messages');
        const closeModalBtn = document.querySelector('.close-modal');
        let isMessagesVisible = false;
        let selectedProductId = null;
        let selectedUserId = null;
        let deleteType = null; // 'product' or 'user'

        // Get messages data from the data attribute
        const contactMessages = JSON.parse('<%- JSON.stringify(contactMessages || []) %>');

        // Tab switching
        document.querySelectorAll('.admin-tab').forEach(tab => {
            tab.addEventListener('click', () => {
                // Remove active class from all tabs
                document.querySelectorAll('.admin-tab').forEach(t => t.classList.remove('active'));
                // Add active class to clicked tab
                tab.classList.add('active');
                
                // Hide all tab content
                document.querySelectorAll('.tab-content').forEach(content => {
                    content.style.display = 'none';
                });
                
                // Show selected tab content
                const tabId = tab.getAttribute('data-tab') + '-tab';
                document.getElementById(tabId).style.display = 'block';
            });
        });

        // Toggle messages container
        notificationBtn.addEventListener('click', () => {
            isMessagesVisible = !isMessagesVisible;
            messagesContainer.classList.toggle('show', isMessagesVisible);
            
            // Rotate bell icon when messages are visible
            notificationBtn.querySelector('i').style.transform = isMessagesVisible ? 'rotate(15deg)' : 'rotate(0)';
        });

        // Close messages container
        closeMessagesBtn.addEventListener('click', () => {
            isMessagesVisible = false;
            messagesContainer.classList.remove('show');
            notificationBtn.querySelector('i').style.transform = 'rotate(0)';
        });

        // Close messages when clicking outside
        document.addEventListener('click', (event) => {
            if (isMessagesVisible && 
                !messagesContainer.contains(event.target) && 
                !notificationBtn.contains(event.target)) {
                isMessagesVisible = false;
                messagesContainer.classList.remove('show');
                notificationBtn.querySelector('i').style.transform = 'rotate(0)';
            }
        });

        // Add click event to message items
        document.querySelectorAll('.message-item').forEach(item => {
            item.addEventListener('click', () => {
                const messageId = item.getAttribute('data-message-id');
                if (messageId) {
                    const message = contactMessages.find(m => m.id == messageId);
                    if (message) {
                        // Populate modal with message data
                        document.getElementById('modal-sender').textContent = message.name;
                        document.getElementById('modal-email').textContent = message.email;
                        document.getElementById('modal-subject').textContent = message.subject;
                        document.getElementById('modal-date').textContent = new Date(message.created_at).toLocaleString();
                        document.getElementById('modal-message').textContent = message.message;
                        
                        // Show modal
                        messageDetailsModal.classList.add('show');
                    }
                }
            });
        });

        // Close modal when clicking close button
        closeModalBtn.addEventListener('click', () => {
            messageDetailsModal.classList.remove('show');
        });

        // Close modal when clicking outside
        messageDetailsModal.addEventListener('click', (event) => {
            if (event.target === messageDetailsModal) {
                messageDetailsModal.classList.remove('show');
            }
        });

        // Product deletion
        document.querySelectorAll('.delete-btn[data-product-id]').forEach(btn => {
            btn.addEventListener('click', (event) => {
                event.stopPropagation();
                const productId = btn.getAttribute('data-product-id');
                const productRow = document.querySelector(`tr[data-product-id="${productId}"]`);
                const productName = productRow.querySelector('td:nth-child(2)').textContent;
                
                selectedProductId = productId;
                selectedUserId = null;
                deleteType = 'product';
                
                productToDelete.textContent = `Product: ${productName} (ID: ${productId})`;
                confirmationModal.classList.add('show');
            });
        });

        // User deletion
        document.querySelectorAll('.delete-btn[data-user-id]').forEach(btn => {
            btn.addEventListener('click', (event) => {
                event.stopPropagation();
                
                // Skip if button is disabled (admin user)
                if (btn.disabled) {
                    alert('Admin users cannot be deleted');
                    return;
                }
                
                const userId = btn.getAttribute('data-user-id');
                const userRow = document.querySelector(`tr[data-user-id="${userId}"]`);
                const userName = userRow.querySelector('td:nth-child(2)').textContent;
                
                selectedUserId = userId;
                selectedProductId = null;
                deleteType = 'user';
                
                productToDelete.textContent = `User: ${userName} (ID: ${userId})`;
                confirmationModal.classList.add('show');
            });
        });

        // Close confirmation modal
        closeConfirmationModal.addEventListener('click', () => {
            confirmationModal.classList.remove('show');
        });

        cancelDelete.addEventListener('click', () => {
            confirmationModal.classList.remove('show');
        });

        // Confirm delete
        confirmDelete.addEventListener('click', () => {
            if (deleteType === 'product' && selectedProductId) {
                // Send delete request to server for product
                fetch(`/admin/delete-product/${selectedProductId}`, {
                    method: 'DELETE',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                })
                .then(response => {
                    if (response.ok) {
                        // Remove the product row from the table
                        const productRow = document.querySelector(`tr[data-product-id="${selectedProductId}"]`);
                        if (productRow) {
                            productRow.remove();
                        }
                        
                        // Show success message
                        alert('Product deleted successfully!');
                    } else {
                        alert('Failed to delete product. Please try again.');
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('An error occurred while deleting the product.');
                })
                .finally(() => {
                    confirmationModal.classList.remove('show');
                });
            } else if (deleteType === 'user' && selectedUserId) {
                // Send delete request to server for user
                fetch(`/admin/delete-user/${selectedUserId}`, {
                    method: 'DELETE',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                })
                .then(response => {
                    if (response.ok) {
                        // Remove the user row from the table
                        const userRow = document.querySelector(`tr[data-user-id="${selectedUserId}"]`);
                        if (userRow) {
                            userRow.remove();
                        }
                        
                        // Show success message
                        alert('User deleted successfully!');
                    } else {
                        // Parse the error message from the response
                        return response.json().then(data => {
                            throw new Error(data.error || 'Failed to delete user');
                        });
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert(error.message || 'An error occurred while deleting the user.');
                })
                .finally(() => {
                    confirmationModal.classList.remove('show');
                });
            }
        });
    </script>
</body>
</html> 