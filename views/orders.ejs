<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Orders - Farmix</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #2ecc71;
            --secondary-color: #f1c40f;
            --dark-color: #2c3e50;
            --light-color: #ecf0f1;
            --accent-color: #e74c3c;
            --border-radius: 10px;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8f9fa;
            color: #333;
        }

        .navbar {
            padding: 15px 0;
            background: white;
            box-shadow: 0 2px 15px rgba(0,0,0,0.1);
        }

        .navbar-brand {
            font-size: 28px;
            font-weight: 700;
            color: var(--primary-color) !important;
        }

        .nav-link {
            color: var(--dark-color) !important;
            font-weight: 500;
            margin: 0 12px;
            position: relative;
            transition: color 0.3s;
        }

        .nav-link:hover {
            color: var(--primary-color) !important;
        }

        .nav-link.active {
            color: var(--primary-color) !important;
            font-weight: 600;
        }

        .nav-link.active::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 100%;
            height: 3px;
            background-color: var(--primary-color);
            border-radius: 3px;
        }

        /* Orders Header */
        .orders-header {
            background: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)),
                        url('https://images.unsplash.com/photo-1595665593673-bf1ad72905c9?ixlib=rb-1.2.1&auto=format&fit=crop&w=1350&q=80');
            background-size: cover;
            background-position: center;
            padding: 80px 0;
            color: white;
            text-align: center;
            margin-bottom: 40px;
            position: relative;
        }

        .orders-header::before {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 80px;
            background: linear-gradient(to top, #f8f9fa, transparent);
        }

        .orders-header h1 {
            font-size: 48px;
            font-weight: 700;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }

        .orders-header p {
            font-size: 20px;
            max-width: 600px;
            margin: 0 auto;
            text-shadow: 1px 1px 3px rgba(0,0,0,0.5);
        }

        /* Order Cards */
        .order-card {
            background: white;
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.07);
            margin-bottom: 25px;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .order-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.12);
        }

        .order-header {
            background-color: var(--light-color);
            padding: 15px 20px;
            border-bottom: 1px solid #e0e0e0;
        }

        .order-id {
            color: var(--dark-color);
            font-weight: 600;
            margin: 0;
        }

        .order-date {
            color: #777;
            font-size: 14px;
            margin: 5px 0 0 0;
        }

        .order-body {
            padding: 20px;
        }

        .order-product {
            display: flex;
            align-items: center;
            margin-bottom: 15px;
        }

        .product-image {
            width: 80px;
            height: 80px;
            border-radius: var(--border-radius);
            object-fit: cover;
            margin-right: 15px;
        }

        .product-name {
            font-weight: 600;
            margin: 0 0 5px 0;
            color: var(--dark-color);
        }

        .product-seller {
            color: #777;
            font-size: 14px;
            margin: 0;
        }

        .order-details {
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #e0e0e0;
        }

        .order-detail-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }

        .detail-label {
            color: #777;
            font-weight: 500;
        }

        .detail-value {
            font-weight: 600;
            color: var(--dark-color);
        }

        .order-status {
            display: inline-block;
            padding: 5px 15px;
            border-radius: 50px;
            font-size: 14px;
            font-weight: 600;
            margin-top: 10px;
        }

        .status-pending {
            background-color: #ffeaa7;
            color: #d35400;
        }

        .status-processing {
            background-color: #81ecec;
            color: #00cec9;
        }

        .status-shipped {
            background-color: #74b9ff;
            color: #0984e3;
        }
        
        .status-delivered {
            background-color: #55efc4;
            color: #00b894;
        }

        .status-completed {
            background-color: #55efc4;
            color: #00b894;
        }

        .status-cancelled {
            background-color: #fab1a0;
            color: #d63031;
        }

        .payment-completed {
            background-color: #a3f8bf;
            color: #00b894;
        }

        .payment-pending {
            background-color: #ffeaa7;
            color: #d35400;
        }
        
        .order-footer {
            padding: 15px 20px;
            background-color: #f9f9f9;
            border-top: 1px solid #e0e0e0;
            text-align: right;
        }

        .btn-order-detail {
            background-color: var(--primary-color);
            color: white;
            border: none;
            padding: 8px 20px;
            border-radius: 50px;
            font-weight: 500;
            font-size: 14px;
            transition: all 0.3s;
        }

        .btn-order-detail:hover {
            background-color: #27ae60;
            color: white;
        }

        .empty-orders {
            text-align: center;
            padding: 50px 0;
        }

        .empty-icon {
            font-size: 60px;
            color: #ccc;
            margin-bottom: 20px;
        }

        .btn-shop-now {
            background-color: var(--primary-color);
            color: white;
            padding: 12px 30px;
            border-radius: 50px;
            font-weight: 600;
            display: inline-block;
            text-decoration: none;
            margin-top: 20px;
            transition: all 0.3s;
        }

        .btn-shop-now:hover {
            background-color: #27ae60;
            transform: translateY(-3px);
            color: white;
        }

        /* Responsive styles */
        @media (max-width: 768px) {
            .orders-header h1 {
                font-size: 32px;
            }
            
            .orders-header p {
                font-size: 16px;
            }
            
            .product-image {
                width: 60px;
                height: 60px;
            }
        }

        /* Styling for multi-item orders */
        .multi-items {
            display: flex;
            flex-direction: column;
        }
        
        .item-count {
            font-weight: bold;
            margin-bottom: 5px;
            color: #555;
        }
        
        .item-name {
            margin-bottom: 3px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            max-width: 250px;
        }
        
        .more-items {
            font-style: italic;
            color: #888;
            font-size: 0.9rem;
        }

        /* Order statistics styling */
        .order-stats-card {
            background-color: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-bottom: 25px;
        }

        .stat-item {
            text-align: center;
            padding: 15px 10px;
            border-radius: 10px;
            background-color: #f8f9fa;
            transition: all 0.3s ease;
        }

        .stat-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            background-color: #e9f7ef;
        }

        .stat-item h3 {
            font-size: 24px;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 5px;
        }

        .stat-item p {
            font-size: 14px;
            color: #7f8c8d;
            margin-bottom: 0;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="/">Farmix</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/shop">Shop</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/products">Products</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/orders">My Orders</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/about">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contact">Contact</a>
                    </li>
                    <% if (isLoggedIn) { %>
                    <li class="nav-item">
                        <a class="nav-link btn btn-login" href="/logout">Logout</a>
                    </li>
                    <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link btn btn-login" href="/login">Login</a>
                    </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Orders Header -->
    <section class="orders-header">
        <div class="container">
            <h1>My Orders</h1>
            <p>Track and manage your purchases</p>
        </div>
    </section>

    <!-- Orders Content -->
    <div class="container mb-5">
        <div class="row">
            <div class="col-12 mb-4">
                <div class="order-stats-card">
                    <div class="row">
                        <div class="col-md-3">
                            <div class="stat-item">
                                <h3><%= orderStats.total_orders %></h3>
                                <p>Total Orders</p>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="stat-item">
                                <h3>₹<%= Number(orderStats.total_spent).toFixed(2) %></h3>
                                <p>Total Spent</p>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="stat-item">
                                <h3><%= orderStats.single_item_orders %></h3>
                                <p>Single-Item Orders</p>
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="stat-item">
                                <h3><%= orderStats.multi_item_orders %></h3>
                                <p>Multi-Item Orders</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="col-12">
                <% if (orders && orders.length > 0) { %>
                    <% orders.forEach(order => { %>
                        <div class="order-card">
                            <div class="order-header">
                                <h4 class="order-id">Order #<%= order.order_id %></h4>
                                <p class="order-date">Placed on <%= new Date(order.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></p>
                            </div>
                            <div class="order-body">
                                <% if (order.is_multi_item && order.items && order.items.length > 0) { %>
                                    <h5 class="mb-3">Multi-Item Order</h5>
                                    <div class="multi-item-order">
                                        <div class="item-count mb-2"><%= order.items.length %> items in this order</div>
                                        <% let displayItems = order.items.slice(0, 3); %>
                                        <% displayItems.forEach(item => { %>
                                            <div class="order-product mb-2">
                                                <img src="https://via.placeholder.com/80x80" alt="<%= item.product_name %>" class="product-image">
                                                <div class="product-info">
                                                    <h5 class="product-name"><%= item.product_name %></h5>
                                                    <p class="product-seller">Seller: <%= item.seller_name %></p>
                                                    <p class="mb-0">Quantity: <%= item.quantity %> | Price: ₹<%= Number(item.price).toFixed(2) %></p>
                                                </div>
                                            </div>
                                        <% }); %>
                                        <% if (order.items.length > 3) { %>
                                            <div class="more-items text-center my-2">
                                                + <%= order.items.length - 3 %> more items
                                            </div>
                                        <% } %>
                                    </div>
                                <% } else { %>
                                    <div class="order-product">
                                        <img src="https://via.placeholder.com/80x80" alt="<%= order.Product ? order.Product.name : 'Product' %>" class="product-image">
                                        <div class="product-info">
                                            <h5 class="product-name"><%= order.Product ? order.Product.name : 'Product' %></h5>
                                            <p class="product-seller">Seller: <%= order.Seller ? order.Seller.name : 'Unknown Seller' %></p>
                                        </div>
                                    </div>
                                <% } %>
                                <div class="order-details">
                                    <div class="order-detail-row">
                                        <span class="detail-label">Quantity:</span>
                                        <span class="detail-value"><%= order.quantity %></span>
                                    </div>
                                    <div class="order-detail-row">
                                        <span class="detail-label">Price per unit:</span>
                                        <span class="detail-value">₹<%= order.Product && order.quantity ? (Number(order.total_price) / Number(order.quantity)).toFixed(2) : '0.00' %></span>
                                    </div>
                                    <div class="order-detail-row">
                                        <span class="detail-label">Total Amount:</span>
                                        <span class="detail-value">₹<%= order.total_price ? Number(order.total_price).toFixed(2) : '0.00' %></span>
                                    </div>
                                    <div class="order-detail-row">
                                        <span class="detail-label">Shipping Address:</span>
                                        <span class="detail-value"><%= order.address || 'No address' %><%= order.city ? ', ' + order.city : '' %><%= order.pincode ? ', ' + order.pincode : '' %></span>
                                    </div>
                                    <div class="order-detail-row">
                                        <span class="detail-label">Status:</span>
                                        <span class="order-status status-<%= order.order_status ? order.order_status.toLowerCase() : 'pending' %>"><%= order.order_status ? (order.order_status.charAt(0).toUpperCase() + order.order_status.slice(1)) : 'Pending' %></span>
                                    </div>
                                    <div class="order-detail-row">
                                        <span class="detail-label">Payment:</span>
                                        <span class="order-status payment-<%= order.payment_status ? order.payment_status.toLowerCase() : 'pending' %>"><%= order.payment_status ? (order.payment_status.charAt(0).toUpperCase() + order.payment_status.slice(1)) : 'Pending' %></span>
                                    </div>
                                </div>
                            </div>
                            <div class="order-footer">
                                <a href="/order-detail/<%= order.order_id %>" class="btn btn-outline-primary">View Details</a>
                                <a href="/order-tracking/<%= order.order_id %>" class="btn btn-outline-success"><i class="fas fa-truck-loading"></i> Track Order</a>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="empty-orders">
                        <i class="fas fa-shopping-basket empty-icon"></i>
                        <h3>You haven't placed any orders yet</h3>
                        <p>Explore our marketplace and find fresh agricultural products.</p>
                        <a href="/shop" class="btn-shop-now">Shop Now</a>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-dark text-light py-5 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h5>About Farmix</h5>
                    <p>Your trusted agricultural marketplace connecting farmers and buyers.</p>
                </div>
                <div class="col-md-4">
                    <h5>Quick Links</h5>
                    <ul class="list-unstyled">
                        <li><a href="/" class="text-light">Home</a></li>
                        <li><a href="/shop" class="text-light">Shop</a></li>
                        <li><a href="/products" class="text-light">Products</a></li>
                        <li><a href="/services" class="text-light">Services</a></li>
                        <li><a href="/about" class="text-light">About</a></li>
                        <li><a href="/contact" class="text-light">Contact</a></li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h5>Contact Us</h5>
                    <p>Email: info@farmix.com<br>
                    Phone: +1 234 567 890</p>
                </div>
            </div>
            <div class="text-center mt-4">
                <p>&copy; 2024 Farmix. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 