<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - Agricultural Marketplace</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            height: 100vh;
            margin: 0;
            background: #0a0a0a;
        }
        .register-container {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: url('https://images.unsplash.com/photo-1500382017468-9049fed747ef?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1489&q=80') center/cover;
        }
        .register-card {
            background: rgba(23, 15, 41, 0.9);
            border-radius: 15px;
            overflow: hidden;
            width: 900px;
            height: 600px;
            display: flex;
            box-shadow: 0 0 40px rgba(148, 87, 235, 0.2);
        }
        .register-form {
            width: 50%;
            padding: 40px;
            color: white;
        }
        .welcome-side {
            width: 50%;
            padding: 40px;
            background: linear-gradient(135deg, rgba(148, 87, 235, 0.8), rgba(91, 44, 175, 0.8));
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: white;
            text-align: center;
        }
        .form-control, .form-select {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: white;
            border-radius: 8px;
            padding: 12px;
            margin-bottom: 20px;
        }
        .form-control:focus, .form-select:focus {
            background: rgba(255, 255, 255, 0.15);
            border-color: #9457eb;
            box-shadow: 0 0 0 2px rgba(148, 87, 235, 0.2);
            color: white;
        }
        .form-control::placeholder {
            color: rgba(255, 255, 255, 0.6);
        }
        .form-select {
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 16 16'%3e%3cpath fill='none' stroke='%23ffffff' stroke-linecap='round' stroke-linejoin='round' stroke-width='2' d='m2 5 6 6 6-6'/%3e%3c/svg%3e");
        }
        .form-select option {
            background: rgba(23, 15, 41, 1);
            color: white;
        }
        .btn-register {
            background: #9457eb;
            border: none;
            border-radius: 8px;
            padding: 12px;
            width: 100%;
            color: white;
            font-weight: 600;
            margin-top: 10px;
        }
        .btn-register:hover {
            background: #7c3cd6;
            color: white;
        }
        .register-header {
            margin-bottom: 30px;
        }
        .register-header h1 {
            font-size: 28px;
            margin-bottom: 10px;
        }
        .login-link {
            margin-top: 20px;
            text-align: center;
            color: rgba(255, 255, 255, 0.7);
        }
        .login-link a {
            color: #9457eb;
            text-decoration: none;
        }
        .login-link a:hover {
            text-decoration: underline;
        }
        .error-message {
            color: #ff6b6b;
            margin-bottom: 15px;
            text-align: center;
        }
        .welcome-side h2 {
            font-size: 32px;
            margin-bottom: 20px;
        }
        .welcome-side p {
            font-size: 16px;
            opacity: 0.9;
            max-width: 80%;
        }
    </style>
</head>
<body>
    <div class="register-container">
        <div class="register-card">
            <div class="register-form">
                <div class="register-header">
                    <h1>Create Account</h1>
                </div>

                <% if (error) { %>
                    <div class="error-message">
                        <%= error %>
                    </div>
                <% } %>

                <form action="/register" method="POST">
                    <input type="text" class="form-control" name="name" placeholder="Full Name" required>
                    <input type="email" class="form-control" name="email" placeholder="Email" required>
                    <input type="password" class="form-control" name="password" placeholder="Password" required>
                    <input type="password" class="form-control" name="confirmPassword" placeholder="Confirm Password" required>
                    <select class="form-select" name="role" required>
                        <option value="" disabled selected>I am a...</option>
                        <option value="farmer">Farmer</option>
                        <option value="buyer">Buyer</option>
                    </select>
                    <button type="submit" class="btn btn-register">Create Account</button>
                </form>

                <div class="login-link">
                    Already have an account? <a href="/login">Login</a>
                </div>
            </div>
            <div class="welcome-side">
                <h2>Join Our Community!</h2>
                <p>Connect with farmers and buyers in our agricultural marketplace. Start your journey today!</p>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 